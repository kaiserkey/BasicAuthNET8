### Test de API REST con Postman y Basic Auth

Basic Authentication (o Basic Auth) es un método de autenticación que envía las credenciales del usuario (nombre de usuario y contraseña) codificadas en Base64 dentro del encabezado Authorization de cada solicitud HTTP.